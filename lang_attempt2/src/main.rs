// Imported in order of usage
mod repl;
mod lexer;
mod ast;
mod tests;

fn main() {
    // repl::start();
    // let input = "x + y; z == 4/! if x = 2".as_bytes();
    // let tokens = lexer::tokenize(input);
    // let ast = ast::build(&tokens);
    // tests::test_advanced_token();
}
